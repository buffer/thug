
import logging
log = logging.getLogger("Thug")

def definition(arg):
    log.ThugLogging.log_exploit_event(self._window.url, #pylint:disable=undefined-name
                                      "MSXML2.DOMDocument",
                                      "Microsoft XML Core Services MSXML Uninitialized Memory Corruption",
                                      cve = "CVE-2012-1889")
